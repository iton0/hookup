package internal

// we can set a relative path to the hookup folder
// ex. git config --local core.hooksPath ./.hookup

// for worktree specify which worktree to look and update the hookPath to
// something like hu init --worktree <worktree repo>
// they can leave blank in which the hu will look at all the available worktrees to find a .hookup file
